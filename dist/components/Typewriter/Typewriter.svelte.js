import './Typewriter.svelte.css';
/* src/components/Typewriter/Typewriter.svelte generated by Svelte v3.32.3 */
import {
	SvelteComponent,
	action_destroyer,
	attr,
	check_outros,
	create_slot,
	detach,
	element,
	empty,
	group_outros,
	init,
	insert,
	is_function,
	noop,
	safe_not_equal,
	set_style,
	toggle_class,
	transition_in,
	transition_out,
	update_slot
} from "../../../_snowpack/pkg/svelte/internal.js";

import { onMount, beforeUpdate, onDestroy, createEventDispatcher } from "../../../_snowpack/pkg/svelte.js";
import { typewriter } from "./modes/index.js";

function create_key_block(ctx) {
	let div;
	let typewriter_action;
	let current;
	let mounted;
	let dispose;
	const default_slot_template = /*#slots*/ ctx[9].default;
	const default_slot = create_slot(default_slot_template, ctx, /*$$scope*/ ctx[8], null);

	return {
		c() {
			div = element("div");
			if (default_slot) default_slot.c();
			attr(div, "class", "typewriter-container svelte-1otnss0");

			set_style(div, "--cursor-color", typeof /*cursor*/ ctx[0] === "string"
			? /*cursor*/ ctx[0]
			: "black");

			toggle_class(div, "cursor", /*cursor*/ ctx[0]);
			toggle_class(div, "delay", /*options*/ ctx[2].delay > 0);
		},
		m(target, anchor) {
			insert(target, div, anchor);

			if (default_slot) {
				default_slot.m(div, null);
			}

			current = true;

			if (!mounted) {
				dispose = action_destroyer(typewriter_action = typewriter.call(null, div, /*options*/ ctx[2]));
				mounted = true;
			}
		},
		p(ctx, dirty) {
			if (default_slot) {
				if (default_slot.p && dirty & /*$$scope*/ 256) {
					update_slot(default_slot, default_slot_template, ctx, /*$$scope*/ ctx[8], dirty, null, null);
				}
			}

			if (!current || dirty & /*cursor*/ 1) {
				set_style(div, "--cursor-color", typeof /*cursor*/ ctx[0] === "string"
				? /*cursor*/ ctx[0]
				: "black");
			}

			if (typewriter_action && is_function(typewriter_action.update) && dirty & /*options*/ 4) typewriter_action.update.call(null, /*options*/ ctx[2]);

			if (dirty & /*cursor*/ 1) {
				toggle_class(div, "cursor", /*cursor*/ ctx[0]);
			}

			if (dirty & /*options*/ 4) {
				toggle_class(div, "delay", /*options*/ ctx[2].delay > 0);
			}
		},
		i(local) {
			if (current) return;
			transition_in(default_slot, local);
			current = true;
		},
		o(local) {
			transition_out(default_slot, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(div);
			if (default_slot) default_slot.d(detaching);
			mounted = false;
			dispose();
		}
	};
}

function create_fragment(ctx) {
	let previous_key = /*reinit*/ ctx[1];
	let key_block_anchor;
	let current;
	let key_block = create_key_block(ctx);

	return {
		c() {
			key_block.c();
			key_block_anchor = empty();
		},
		m(target, anchor) {
			key_block.m(target, anchor);
			insert(target, key_block_anchor, anchor);
			current = true;
		},
		p(ctx, [dirty]) {
			if (dirty & /*reinit*/ 2 && safe_not_equal(previous_key, previous_key = /*reinit*/ ctx[1])) {
				group_outros();
				transition_out(key_block, 1, 1, noop);
				check_outros();
				key_block = create_key_block(ctx);
				key_block.c();
				transition_in(key_block);
				key_block.m(key_block_anchor.parentNode, key_block_anchor);
			} else {
				key_block.p(ctx, dirty);
			}
		},
		i(local) {
			if (current) return;
			transition_in(key_block);
			current = true;
		},
		o(local) {
			transition_out(key_block);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(key_block_anchor);
			key_block.d(detaching);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let options;
	let { $$slots: slots = {}, $$scope } = $$props;
	let { interval = 30 } = $$props;
	let { cascade = false } = $$props;
	let { loop = false } = $$props;
	let { loopRandom = false } = $$props;
	let { cursor = true } = $$props;
	let { delay = 0 } = $$props;
	let isMounted = false;
	let reinit = {};
	const dispatch = createEventDispatcher();
	beforeUpdate(() => isMounted && $$invalidate(1, reinit = {}));
	onMount(() => isMounted = true);

	$$self.$$set = $$props => {
		if ("interval" in $$props) $$invalidate(3, interval = $$props.interval);
		if ("cascade" in $$props) $$invalidate(4, cascade = $$props.cascade);
		if ("loop" in $$props) $$invalidate(5, loop = $$props.loop);
		if ("loopRandom" in $$props) $$invalidate(6, loopRandom = $$props.loopRandom);
		if ("cursor" in $$props) $$invalidate(0, cursor = $$props.cursor);
		if ("delay" in $$props) $$invalidate(7, delay = $$props.delay);
		if ("$$scope" in $$props) $$invalidate(8, $$scope = $$props.$$scope);
	};

	$$self.$$.update = () => {
		if ($$self.$$.dirty & /*interval, cascade, loop, loopRandom, cursor, delay*/ 249) {
			$: $$invalidate(2, options = {
				interval,
				cascade,
				loop,
				loopRandom,
				cursor,
				delay,
				dispatch
			});
		}
	};

	return [
		cursor,
		reinit,
		options,
		interval,
		cascade,
		loop,
		loopRandom,
		delay,
		$$scope,
		slots
	];
}

class Typewriter extends SvelteComponent {
	constructor(options) {
		super();

		init(this, options, instance, create_fragment, safe_not_equal, {
			interval: 3,
			cascade: 4,
			loop: 5,
			loopRandom: 6,
			cursor: 0,
			delay: 7
		});
	}
}

export default Typewriter;