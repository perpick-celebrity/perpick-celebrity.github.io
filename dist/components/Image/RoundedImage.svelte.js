import './RoundedImage.svelte.css';
/* src/components/Image/RoundedImage.svelte generated by Svelte v3.32.3 */
import {
	SvelteComponent,
	attr,
	binding_callbacks,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	toggle_class
} from "../../../_snowpack/pkg/svelte/internal.js";

import { onMount } from "../../../_snowpack/pkg/svelte.js";

function create_fragment(ctx) {
	let img;
	let img_src_value;

	return {
		c() {
			img = element("img");
			if (img.src !== (img_src_value = /*src*/ ctx[0])) attr(img, "src", img_src_value);
			attr(img, "alt", /*alt*/ ctx[1]);
			attr(img, "class", "rounded mx-auto svelte-1e2i2ui");
			toggle_class(img, "loaded", /*loaded*/ ctx[2]);
		},
		m(target, anchor) {
			insert(target, img, anchor);
			/*img_binding*/ ctx[4](img);
		},
		p(ctx, [dirty]) {
			if (dirty & /*src*/ 1 && img.src !== (img_src_value = /*src*/ ctx[0])) {
				attr(img, "src", img_src_value);
			}

			if (dirty & /*alt*/ 2) {
				attr(img, "alt", /*alt*/ ctx[1]);
			}

			if (dirty & /*loaded*/ 4) {
				toggle_class(img, "loaded", /*loaded*/ ctx[2]);
			}
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(img);
			/*img_binding*/ ctx[4](null);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { src } = $$props;
	let { alt } = $$props;
	let loaded = false;
	let thisImage;

	onMount(() => {
		$$invalidate(
			3,
			thisImage.onload = () => {
				$$invalidate(2, loaded = true);
			},
			thisImage
		);
	});

	function img_binding($$value) {
		binding_callbacks[$$value ? "unshift" : "push"](() => {
			thisImage = $$value;
			$$invalidate(3, thisImage);
		});
	}

	$$self.$$set = $$props => {
		if ("src" in $$props) $$invalidate(0, src = $$props.src);
		if ("alt" in $$props) $$invalidate(1, alt = $$props.alt);
	};

	return [src, alt, loaded, thisImage, img_binding];
}

class RoundedImage extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { src: 0, alt: 1 });
	}
}

export default RoundedImage;