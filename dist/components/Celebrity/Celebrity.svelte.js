import './Celebrity.svelte.css';
/* src/components/Celebrity/Celebrity.svelte generated by Svelte v3.32.3 */
import {
	SvelteComponent,
	append,
	attr,
	detach,
	element,
	empty,
	init,
	insert,
	noop,
	safe_not_equal,
	set_data,
	space,
	text
} from "../../../_snowpack/pkg/svelte/internal.js";

function create_if_block(ctx) {
	let div1;
	let h1;
	let t0_value = /*celebrity*/ ctx[0].title + "";
	let t0;
	let t1;
	let div0;

	return {
		c() {
			div1 = element("div");
			h1 = element("h1");
			t0 = text(t0_value);
			t1 = space();
			div0 = element("div");
			attr(h1, "class", "m-2 text-2xl text-pink-400 svelte-ytar5m");
			attr(div1, "class", "m-4 divide-y-2 divide-gray-200 tracking-widest init_anim svelte-ytar5m");
		},
		m(target, anchor) {
			insert(target, div1, anchor);
			append(div1, h1);
			append(h1, t0);
			append(div1, t1);
			append(div1, div0);
		},
		p(ctx, dirty) {
			if (dirty & /*celebrity*/ 1 && t0_value !== (t0_value = /*celebrity*/ ctx[0].title + "")) set_data(t0, t0_value);
		},
		d(detaching) {
			if (detaching) detach(div1);
		}
	};
}

function create_fragment(ctx) {
	let if_block_anchor;
	let if_block = /*celebrity*/ ctx[0] && create_if_block(ctx);

	return {
		c() {
			if (if_block) if_block.c();
			if_block_anchor = empty();
		},
		m(target, anchor) {
			if (if_block) if_block.m(target, anchor);
			insert(target, if_block_anchor, anchor);
		},
		p(ctx, [dirty]) {
			if (/*celebrity*/ ctx[0]) {
				if (if_block) {
					if_block.p(ctx, dirty);
				} else {
					if_block = create_if_block(ctx);
					if_block.c();
					if_block.m(if_block_anchor.parentNode, if_block_anchor);
				}
			} else if (if_block) {
				if_block.d(1);
				if_block = null;
			}
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (if_block) if_block.d(detaching);
			if (detaching) detach(if_block_anchor);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { celebrity } = $$props;

	$$self.$$set = $$props => {
		if ("celebrity" in $$props) $$invalidate(0, celebrity = $$props.celebrity);
	};

	return [celebrity];
}

class Celebrity extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { celebrity: 0 });
	}
}

export default Celebrity;