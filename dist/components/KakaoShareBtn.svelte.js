import * as __SNOWPACK_ENV__ from '../../_snowpack/env.js';

import './KakaoShareBtn.svelte.css';
/* src/components/KakaoShareBtn.svelte generated by Svelte v3.32.3 */
import {
	SvelteComponent,
	attr,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal
} from "../../_snowpack/pkg/svelte/internal.js";

import { onMount } from "../../_snowpack/pkg/svelte.js";

function create_fragment(ctx) {
	let button;

	return {
		c() {
			button = element("button");
			button.innerHTML = `<img src="//developers.kakao.com/assets/img/about/logos/kakaolink/kakaolink_btn_medium.png" alt="kakao-share-icon" class="svelte-gdy8gt"/>`;
			attr(button, "id", "kakao-link-btn");
		},
		m(target, anchor) {
			insert(target, button, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(button);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { total = 326872 } = $$props;

	onMount(() => {
		if (window.Kakao) {
			const kakao = window.Kakao;

			if (!kakao.isInitialized()) {
				// 두번째 step 에서 가져온 javascript key 를 이용하여 initialize
				Kakao.init(__SNOWPACK_ENV__.SNOWPACK_PUBLIC_KAKAO_KEY);
			}

			Kakao.Link.createDefaultButton({
				container: "#kakao-link-btn",
				objectType: "feed",
				content: {
					title: "퍼픽 - 나만의 향수 추천",
					description: "나만을 위한 향수를 추천받으세요! #나만의향수 #인공지능 #추천 #분위기 #추억 #기념일",
					imageUrl: "http://k.kakaocdn.net/dn/Fz36y/btqYHTDb7l2/FAbm5E6xyXB3nMVjWu1Prk/kakaolink40_original.jpg",
					link: {
						mobileWebUrl: "https://perpick-celebrity.github.io",
						webUrl: "https://perpick-celebrity.github.io"
					}
				},
				social: {
					likeCount: total,
					commentCount: 45,
					sharedCount: 845
				},
				buttons: [
					{
						title: "당장 추천받아 보기",
						link: {
							mobileWebUrl: "https://perpick-celebrity.github.io",
							webUrl: "https://perpick-celebrity.github.io"
						}
					}
				]
			});
		}
	});

	$$self.$$set = $$props => {
		if ("total" in $$props) $$invalidate(0, total = $$props.total);
	};

	return [total];
}

class KakaoShareBtn extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { total: 0 });
	}
}

export default KakaoShareBtn;