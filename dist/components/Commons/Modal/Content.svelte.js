import './Content.svelte.css';
/* src/components/Commons/Modal/Content.svelte generated by Svelte v3.32.3 */
import {
	SvelteComponent,
	append,
	attr,
	detach,
	element,
	init,
	insert,
	listen,
	noop,
	safe_not_equal,
	set_data,
	set_style,
	text
} from "../../../../_snowpack/pkg/svelte/internal.js";

import { getContext } from "../../../../_snowpack/pkg/svelte.js";
import PopupLong from "./PopupLong.svelte.js";

function create_fragment(ctx) {
	let section;
	let button;
	let t;
	let mounted;
	let dispose;

	return {
		c() {
			section = element("section");
			button = element("button");
			t = text(/*title*/ ctx[0]);
			attr(button, "type", "button");
			set_style(button, "transition", "all .15s ease");
			attr(button, "class", "bg-white text-pink-400 border-b-2 active:bg-pp-600 active:text-white font-bold uppercase text-sm px-2 hover:text-pink-500 transform hover:-translate-y-1 hover:scale-110 hover:border-pink-200 outline-none focus:outline-none svelte-1avlwns");
			attr(section, "class", "text-center svelte-1avlwns");
		},
		m(target, anchor) {
			insert(target, section, anchor);
			append(section, button);
			append(button, t);

			if (!mounted) {
				dispose = listen(button, "click", /*showPopupLong*/ ctx[1]);
				mounted = true;
			}
		},
		p(ctx, [dirty]) {
			if (dirty & /*title*/ 1) set_data(t, /*title*/ ctx[0]);
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(section);
			mounted = false;
			dispose();
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { title } = $$props, { image } = $$props, { rows } = $$props;
	const { open } = getContext("simple-modal");

	const showPopupLong = () => {
		open(PopupLong, { image, rows });
	};

	$$self.$$set = $$props => {
		if ("title" in $$props) $$invalidate(0, title = $$props.title);
		if ("image" in $$props) $$invalidate(2, image = $$props.image);
		if ("rows" in $$props) $$invalidate(3, rows = $$props.rows);
	};

	return [title, showPopupLong, image, rows];
}

class Content extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { title: 0, image: 2, rows: 3 });
	}
}

export default Content;